apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "mmp-watsons-member.fullname" . }}-test-connection"
  labels:
    {{- include "mmp-watsons-member.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "mmp-watsons-member.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
